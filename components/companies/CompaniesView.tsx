"use client";

import { companies } from "@/data/mockCompanies";
import { contacts } from "@/data/mockContacts";
import type { Company } from "@/types";
import { ArrowRight, Building2, ChevronDown, Globe2, MapPin, MoreHorizontal, Plus, Search, UsersRound, X } from "lucide-react";
import { useMemo, useRef, useState } from "react";
import { useDrawerAccessibility } from "@/components/shared/useDrawerAccessibility";

const shortDateFormatter = new Intl.DateTimeFormat("en", {
  month: "short",
  day: "numeric",
  timeZone: "UTC",
});

export function CompaniesView() {
  const [search, setSearch] = useState("");
  const [company, setCompany] = useState<Company | null>(null);
  const visible = useMemo(()=>companies.filter(item=>`${item.name} ${item.industry} ${item.location}`.toLowerCase().includes(search.toLowerCase())),[search]);

  return <div className="space-y-5">
    <div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="section-eyebrow">Organizations</p><h1 className="mt-2 text-[28px] font-medium tracking-[-.04em]">Companies</h1><p className="mt-1.5 text-sm text-[var(--text-secondary)]">10 organizations connected to your contacts</p></div><button className="btn btn-primary w-fit gap-2"><Plus size={14}/>Add company</button></div>
    <section className="grid gap-3 sm:grid-cols-3">{[[Building2,"10","Companies"],[UsersRound,"24,821","Linked contacts"],[Globe2,"8","Industries"]].map(([Icon,value,label])=>{const MetricIcon=Icon as typeof Building2;return <article key={label as string} className="card flex items-center gap-4 p-4"><span className="grid size-10 place-items-center rounded-xl bg-[#f0efff] text-[#625cf6]"><MetricIcon size={18}/></span><div><p className="text-[21px] font-semibold tracking-[-.035em]">{value as string}</p><p className="text-[9px] text-[var(--text-tertiary)]">{label as string}</p></div></article>})}</section>
    <section className="card overflow-hidden"><div className="flex flex-wrap items-center gap-2 border-b border-[var(--border)] p-3"><label className="relative min-w-[220px] flex-1"><span className="sr-only">Search companies</span><Search size={14} className="absolute left-3 top-1/2 -translate-y-1/2 text-[var(--text-tertiary)]"/><input value={search} onChange={e=>setSearch(e.target.value)} className="input h-9 w-full pl-9 text-[11px]" placeholder="Search companies"/></label><button className="btn btn-secondary gap-2">Industry <ChevronDown size={12}/></button><button className="btn btn-secondary gap-2">Location <ChevronDown size={12}/></button><button className="btn btn-ghost px-2" aria-label="More company filters"><MoreHorizontal size={16}/></button></div><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left"><thead><tr className="bg-[var(--surface-subtle)] text-[9px] font-semibold uppercase tracking-[.07em] text-[var(--text-tertiary)]"><th className="px-5 py-3">Company</th><th className="px-4 py-3">Industry</th><th className="px-4 py-3">Contacts</th><th className="px-4 py-3">Location</th><th className="px-4 py-3">Owner</th><th className="px-4 py-3">Last activity</th><th className="w-10"/></tr></thead><tbody>{visible.map(item=><tr key={item.id} className="border-t border-[var(--border)] hover:bg-[var(--surface-subtle)]"><td className="px-5 py-3.5"><button onClick={()=>setCompany(item)} className="flex items-center gap-3 text-left"><span className="grid size-9 place-items-center rounded-xl text-[10px] font-semibold text-white" style={{backgroundColor:item.brandColor}}>{item.initials}</span><span><span className="block text-[10px] font-semibold hover:text-[#625cf6]">{item.name}</span><span className="mt-0.5 block text-[8px] text-[var(--text-tertiary)]">{item.domain}</span></span></button></td><td className="px-4 py-3.5 text-[10px] text-[var(--text-secondary)]">{item.industry}</td><td className="px-4 py-3.5"><span className="badge badge-info">{item.contactsCount} people</span></td><td className="px-4 py-3.5 text-[10px] text-[var(--text-secondary)]">{item.location}</td><td className="px-4 py-3.5 text-[10px]">{item.owner}</td><td className="px-4 py-3.5 text-[9px] text-[var(--text-tertiary)]">{shortDateFormatter.format(new Date(item.lastActivityAt))}</td><td><button aria-label={`More actions for ${item.name}`}><MoreHorizontal size={15} className="text-[var(--text-tertiary)]"/></button></td></tr>)}</tbody></table></div></section>
    {company&&<CompanyDrawer company={company} onClose={()=>setCompany(null)}/>} 
  </div>;
}

function CompanyDrawer({company,onClose}:{company:Company;onClose:()=>void}) {
  const linked=contacts.filter(contact=>contact.companyId===company.id).slice(0,5);
  const panelRef=useRef<HTMLElement>(null);
  useDrawerAccessibility(panelRef,onClose);
  return <div className="fixed inset-0 z-[80] flex justify-end bg-[#171823]/25 backdrop-blur-[2px]" role="presentation" onMouseDown={event=>{if(event.target===event.currentTarget)onClose()}}><section ref={panelRef} tabIndex={-1} role="dialog" aria-modal="true" aria-label={`${company.name} profile`} className="h-full w-full max-w-[540px] overflow-y-auto border-l border-[var(--border)] bg-white shadow-[-22px_0_60px_rgba(30,31,46,.16)]"><div className="sticky top-0 z-10 flex justify-between border-b border-[var(--border)] bg-white/90 px-5 py-3 backdrop-blur-xl"><span className="text-[10px] text-[var(--text-tertiary)]">Company profile</span><button data-autofocus="true" onClick={onClose} className="grid size-8 place-items-center rounded-lg hover:bg-[var(--surface-subtle)]" aria-label="Close company profile"><X size={17}/></button></div><div className="p-6"><div className="flex gap-4"><span className="grid size-14 place-items-center rounded-2xl text-[16px] font-semibold text-white" style={{backgroundColor:company.brandColor}}>{company.initials}</span><div><h2 className="text-[22px] font-semibold tracking-[-.035em]">{company.name}</h2><p className="mt-1 text-[11px] text-[var(--text-secondary)]">{company.industry} · {company.size} employees</p><span className="mt-2 inline-flex badge badge-success">Active account</span></div></div><div className="mt-6 grid grid-cols-2 gap-2"><a href={company.website} className="btn btn-secondary justify-center gap-2"><Globe2 size={13}/>Website</a><a href={`/campaigns/new?company=${company.id}`} className="btn btn-primary justify-center gap-2">Create campaign<ArrowRight size={13}/></a></div><div className="mt-7 grid grid-cols-2 gap-3"><div className="rounded-xl bg-[var(--surface-subtle)] p-4"><p className="text-[9px] text-[var(--text-tertiary)]">Contacts</p><p className="mt-1 text-[21px] font-semibold">{company.contactsCount}</p></div><div className="rounded-xl bg-[var(--surface-subtle)] p-4"><p className="text-[9px] text-[var(--text-tertiary)]">Location</p><p className="mt-1 text-[11px] font-semibold">{company.location}</p></div></div><section className="mt-7"><div className="flex items-center justify-between"><h3 className="text-[12px] font-semibold">People at {company.name}</h3><button className="text-[10px] font-semibold text-[#625cf6]">View all</button></div><div className="mt-3 overflow-hidden rounded-xl border border-[var(--border)]">{linked.length?linked.map(contact=><a key={contact.id} href={`/contacts/${contact.id}`} className="flex items-center gap-3 border-b border-[var(--border)] p-3 last:border-0 hover:bg-[var(--surface-subtle)]"><span className="grid size-8 place-items-center rounded-full text-[9px] font-semibold text-white" style={{backgroundColor:contact.avatarColor}}>{contact.firstName[0]}{contact.lastName[0]}</span><span className="min-w-0 flex-1"><span className="block text-[10px] font-semibold">{contact.fullName}</span><span className="mt-0.5 block truncate text-[8px] text-[var(--text-tertiary)]">{contact.role}</span></span><ArrowRight size={13} className="text-[var(--text-tertiary)]"/></a>):<div className="p-7 text-center"><MapPin size={18} className="mx-auto text-[var(--text-tertiary)]"/><p className="mt-2 text-[10px] text-[var(--text-tertiary)]">No sample contacts linked</p></div>}</div></section></div></section></div>;
}
